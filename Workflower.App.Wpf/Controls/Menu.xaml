<UserControl x:Class="Workflower.App.Wpf.Controls.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Workflower.App.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<controls:IconButton
			Icon="{DynamicResource IconRefresh}"
			ToolTip="Refresh all"
			Click="Refresh_Click"
            />
		<controls:IconButton
			Grid.Column="1"
			Icon="{DynamicResource IconOpen}"
			ToolTip="Change project"
			Click="ChangeProject_Click"
            />
	</Grid>
</UserControl>
